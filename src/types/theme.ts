export interface Theme {
    colors: {
        layout: {
            body: string,
            primary: string,
        },
        text: {
            primary: string,
            secondary: string,
        },
    },
    fontSize: {
        tiny:string,
        small:string,
        normal:string,
        medium: string,
        large: string,
        title: string,
    }
}
